import { YelowBackgroundDirective } from './yelow-background.directive';

describe('YelowBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new YelowBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
