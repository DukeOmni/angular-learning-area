import { FiltroArrayImpurePipe } from './filtro-array-impure.pipe';

describe('FiltroArrayImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArrayImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
